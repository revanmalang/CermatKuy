"""Service layer for CermatKuy.

Modules:
    analyzer          -- local heuristic content analysis (language, style, structure)
    article_extractor -- fetch and parse article content from a URL
    source_checker     -- domain reputation lookup against trusted_sources.json
    fact_checker       -- abstraction layer for external fact-check APIs
"""
